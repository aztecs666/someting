---
phase: 4
wave: 1
depends_on: []
requirements_addressed: [PLAN-03]
autonomous: false
---

# Plan 04-01: Address High-Signal Hygiene Issues

## Goal

Clean up the repository to make it easier to operate safely. Phase 3 fixed integrity issues; this phase removes workflow friction.

## Context

Phase 3 uncovered or left these hygiene issues:
- `.gitignore` is incomplete — `__pycache__/` missing as pattern, `notepad/` not ignored
- `all_programs.txt` and `temp_output.txt` are stray root-level files with no project purpose
- `notepad/` and `skills/` are empty directories — should be removed or ignored
- `real data/shipments.db` is a duplicate of `data/shipments.db` — same filename, different directory

## Tasks

### Task 1: Audit and clean stray root-level files

<read_first>
- .gitignore
</read_first>

<acceptance_criteria>
- `all_programs.txt` does not exist in the repository
- `temp_output.txt` does not exist in the repository
- Both files are confirmed deleted, not just gitignored
</acceptance_criteria>

<action>
Delete the following files from the repository root:
- `all_programs.txt`
- `temp_output.txt`
</action>

### Task 2: Remove orphaned empty directories

<read_first>
- skills/ (confirm empty)
- notepad/ (confirm empty)
</read_first>

<acceptance_criteria>
- `skills/` directory does not exist
- `notepad/` directory does not exist
- No other empty or single-file directories remain at the repository root that serve no project purpose
</acceptance_criteria>

<action>
Remove the following directories if they are empty or contain only non-project files:
- `skills/`
- `notepad/`
</action>

### Task 3: Audit real data duplicate

<read_first>
- data/shipments.db (confirm this is the canonical)
- real data/shipments.db (compare with canonical)
</read_first>

<acceptance_criteria>
- `real data/shipments.db` is either deleted (if duplicate) or moved to `data/` (if containing unique data)
- The canonical database path is `data/shipments.db`
- `real data/` directory is removed if it contains no unique data after consolidation
</acceptance_criteria>

<action>
Compare `real data/shipments.db` against `data/shipments.db`. If they are duplicates or `real data/` contains no unique tables/rows not in `data/shipments.db`:
1. Delete `real data/shipments.db` and `real data/engineered_shipments.db`
2. Remove the `real data/` directory
3. Update .gitignore to remove the `real data/` entry if it was only there to ignore these files

If `real data/` contains unique data, move it to `data/` under a different name and update relevant path references.
</action>

### Task 4: Harden .gitignore

<read_first>
- .gitignore
- app/forecast_support.py (check for hardcoded paths)
- data/real_data_fetcher.py (check for hardcoded paths)
</read_first>

<acceptance_criteria>
- .gitignore contains `__pycache__/` (not just `*.pyc`)
- .gitignore contains `notepad/` if directory is retained
- .gitignore does not contain entries for files that no longer exist
- No generated artifacts (*.joblib, *.json metrics, *.csv training datasets) are tracked by git
</acceptance_criteria>

<action>
Update .gitignore to be complete:
```
__pycache__/
*.pyc
*.joblib
logs/
ml/*.joblib
ml/route_forecaster_metrics.json
ml/route_forecaster_runtime_manifest.json
app/*.joblib
app/*.csv
app/route_forecaster_metrics.json
data/*.db
all_programs.txt
temp_output.txt
real data/
notepad/
skills/
```
Remove entries for files that no longer exist after Tasks 1-3.
</action>

## Verification

Run `git status --short` and confirm:
- No untracked files remain except generated artifacts (ml/*.joblib, data/*.db, logs/*)
- No stray files from the hygiene list above

Run `python utils/real_data_audit.py` to confirm the pipeline still works after path consolidation.

## Must-Haves

- [ ] `all_programs.txt` and `temp_output.txt` deleted
- [ ] `notepad/` and `skills/` directories removed
- [ ] `real data/shipments.db` issue resolved (duplicate deleted or unique data moved)
- [ ] `.gitignore` hardened and entries for deleted files removed
- [ ] Pipeline still functional after path changes (audit runs clean)
- [ ] Atomic commit with message: "chore: tighten repo hygiene — remove stray files and harden gitignore"
